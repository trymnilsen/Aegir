<UserControl x:Class="Aegir.PropertyGrid.PropertyGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aegir.PropertyGrid"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="CategoryPanel">
                
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1">
            <GroupBox>
                <GroupBox.Header>
                    <HeaderedContentControl>
                        <TextBlock Text="Item selected"></TextBlock>
                    </HeaderedContentControl>
                </GroupBox.Header>
                <TextBlock TextWrapping="Wrap" Text="Some description of the selected item here yayayda some more text foobar. This is the place of the description of the selected."></TextBlock>
            </GroupBox>
        </Border>
    </Grid>
</UserControl>
